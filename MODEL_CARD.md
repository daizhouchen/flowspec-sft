# FlowSpec 模型卡

## 模型用途

FlowSpec 使用 Qwen3-1.7B 将中文业务指令编译为 `WorkflowSpec v1` JSON。输出必须经过 Schema、DAG、工具参数、权限与失败路径校验，之后才可进入无副作用沙箱。模型不连接真实邮箱、企业微信、数据库或其他外部写入系统。

## 训练配置

- 基座：`Qwen/Qwen3-1.7B`
- 方法：4-bit QLoRA，LoRA rank 16，attention 与 MLP 投影层
- 数据：1,600 train / 250 dev；12 个训练模板族，覆盖 18 个工具
- 长度与训练：2048 tokens，2 epochs，200 steps
- 资源：共享 RTX 4090，单卡串行运行；峰值分配 4.276 GiB，峰值保留 6.119 GiB
- 结果：训练损失 0.0221，验证损失 0.1914

## 固定评测

| 方案 | Schema | DAG | 沙箱 | 工具 F1 | 参数 F1 | 依赖边 F1 | 语义结构 | 平均延迟 ms |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| zero-shot | 0.668 | 0.668 | 0.500 | 0.2513 | 0.0015 | 0.0000 | 0.0843 | 201.88 |
| few-shot + 校验器 | 0.852 | 0.852 | 0.852 | 0.7061 | 0.7192 | 0.5952 | 0.6735 | 2614.30 |
| QLoRA + 校验器 | **1.000** | **1.000** | **1.000** | **0.8558** | **0.8438** | **0.6795** | **0.7930** | 1825.97 |
| QLoRA 挑战集 | **1.000** | **1.000** | **1.000** | **0.8183** | **0.8008** | 0.3759 | **0.6650** | 2224.00 |

语义结构得分为工具、参数槽位与依赖边 F1 的均值。QLoRA 相对 few-shot 提高 0.1195，达到预设的至少 0.08 增益门槛。指标来自合成数据；标准测试集和挑战集仍为 `pending_user_review`，故全部结果保持 provisional。

## 导出与部署

- Adapter：`artifacts/qwen3-1.7b-qlora-diverse`（远程实验产物）
- CPU 格式：GGUF Q4_K_M，1,107,408,736 bytes
- SHA-256：`cb9ce8cde4b3f733b2c97fa43966517914a841e08a8eaffee303af74962ce0a3`
- CPU 冒烟：2 线程，6.37 秒完成加载与单轮回复
- 端到端：`llama-server` 与 FastAPI 联调通过；5 节点工作流完成生成、校验和沙箱执行

仓库不直接提交 1.1 GB GGUF。`scripts/export_gguf.sh` 可从 Adapter 合并、量化并生成校验文件。

## 局限

训练与评测数据为原创合成工作流，不能替代真实企业流程验证。挑战集依赖边 F1 为 0.3759，说明未见组合中的拓扑泛化仍是主要短板。CPU 端到端样例耗时 70.75 秒，适合复现和受限 Demo，不适合未经优化的高并发部署。
